name: Sedstart QA

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger SedStart test run
        uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY }}
          project_id: 444
          test_id: 2159
          profile_id: 953
          browser: chrome
          headless: false
          environment: QA
